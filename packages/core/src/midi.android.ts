// ─── MIDI stub for React Native (Android) ───────────────────────────────────
// Duplicate for explicit android platform resolution.

export { MidiManager, midiNoteToFrequency, midiNoteToName, frequencyToMidiNote } from './midi.native';
export type { MidiNoteEvent, MidiDevice } from './midi.native';
